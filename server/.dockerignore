# Never send node_modules into the build context.
# Without this, Docker ships thousands of host files to the daemon,
# and COPY . . would overwrite the clean production install npm ci just made.
node_modules

# No secrets in the image — inject these at runtime via docker run --env-file
.env
.env.*

# Not needed inside the container
*.log
.git
